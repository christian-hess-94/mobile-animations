import React from 'react'
import { View, Text, TouchableOpacity, Animated } from 'react-native'

const SceneDrawerBackdrop = props => {
    const {
        headerAnimation,
        subMenuAnimation,
        loadingAnimation,
        contentAnimation,
        footerAnimation,
        drawerAnimation,
        drawerBackdropAnimation,
        drawerItemsAnimation,
        drawerHeaderAnimation,
        duration,

        sceneStatus,

        hasHeader,
        hasSubMenu,
        hasFooter,
        hasTabs,
        hasLeftButton,
        hasRightButton,
        closeMenu,
    } = props
    return (
        <>

        </>
    )
}

export default SceneDrawerBackdrop
